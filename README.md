# your-library

> One sentence that explains what this library does.

## Install

```bash
npm install your-library
```

## Usage

```js
import { something } from 'your-library'

const result = something({ input: 'hello' })
console.log(result)
```

## API

### `something(options)`

| Option | Type | Description |
|--------|------|-------------|
| `input` | `string` | The input value |
| `timeout` | `number` | Timeout in ms (default: `3000`) |

Returns a `Promise<Result>`.

## Contributing

Pull requests are welcome. For major changes, please open an issue first to discuss what you'd like to change.

Make sure to update tests as appropriate.


